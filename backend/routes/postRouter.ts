import express, { Router } from 'express';
// import {  } from '../contollers/user';
var postRouter: Router = express.Router();

postRouter.get('/' );
postRouter.post('/')

module.exports = postRouter;
